# GPU Cloud 设计系统文档

**编制部门**: 设计部  
**编制日期**: 2026-06-30  
**版本**: v1.0

---

## 一、组件库总览

### 1.1 组件分类
| 分类 | 组件 |
|------|------|
| **通用** | Button, Input, Select, Checkbox, Radio, Switch |
| **导航** | Navbar, Sidebar, Tabs, Breadcrumb, Pagination |
| **反馈** | Toast, Modal, Alert, Skeleton, Progress, Spinner |
| **数据展示** | Table, Card, Tag, Badge, Avatar, Timeline, Statistic |
| **表单** | Form, FormItem, DatePicker, Upload, Search |
| **业务** | GPUCard, InstanceRow, BillingRow, MetricChart |

---

## 二、核心组件规范

### 2.1 Button 按钮

| 变体 | 背景色 | 文字色 | 用途 |
|------|--------|--------|------|
| Primary | `#3B82F6` | `#FFFFFF` | 主要操作 |
| Accent | `#06D6A0` | `#000000` | CTA/充值 |
| Secondary | `#1E293B` | `#E5E7EB` | 次要操作 |
| Danger | `#EF4444` | `#FFFFFF` | 删除/释放 |
| Ghost | 透明 | `#E5E7EB` | 辅助操作 |

**尺寸**:
| 尺寸 | 高度 | 内边距 | 字号 |
|------|------|--------|------|
| lg | 48px | 16px 32px | 16px |
| md | 40px | 12px 22px | 14px |
| sm | 32px | 8px 16px | 13px |

### 2.2 Card 卡片

| 状态 | 背景 | 边框 | 阴影 |
|------|------|------|------|
| 默认 | `#111827` | `#1E293B` 1px | 无 |
| 悬浮 | `#1A2332` | `#3B82F6` 1px | `0 4px 12px rgba(59,130,246,0.12)` |
| 选中 | `#1A2332` | `#06D6A0` 1px | 无 |

**内边距**: 24px  
**圆角**: 8px

### 2.3 Tag 标签

| 类型 | 背景 | 文字色 | 用途 |
|------|------|--------|------|
| Running | `rgba(16,185,129,0.12)` | `#10B981` | 运行中 |
| Stopped | `rgba(107,114,128,0.12)` | `#6B7280` | 已停止 |
| Pending | `rgba(245,158,11,0.12)` | `#F59E0B` | 创建中 |
| Error | `rgba(239,68,68,0.12)` | `#EF4444` | 错误 |
| Terminated | `rgba(107,114,128,0.06)` | `#4B5563` | 已释放 |

### 2.4 Table 表格

| 元素 | 样式 |
|------|------|
| 表头背景 | `rgba(255,255,255,0.03)` |
| 表头文字 | `#9CA3AF`, 12px, uppercase |
| 行高 | 56px |
| 行分割线 | `rgba(255,255,255,0.04)` |
| 悬浮行 | `rgba(255,255,255,0.02)` |
| 内边距 | 16px 20px |

### 2.5 Modal 弹窗

| 元素 | 样式 |
|------|------|
| 背景遮罩 | `rgba(0,0,0,0.6)` |
| 弹窗背景 | `#111827` |
| 边框 | `#1E293B` 1px |
| 圆角 | 12px |
| 阴影 | `0 8px 32px rgba(0,0,0,0.2)` |
| 最小宽度 | 480px |
| 最大宽度 | 720px |

### 2.6 Toast 提示

| 类型 | 图标 | 背景 | 文字色 |
|------|------|------|--------|
| Success | ✅ | `rgba(16,185,129,0.12)` | `#10B981` |
| Error | ❌ | `rgba(239,68,68,0.12)` | `#EF4444` |
| Warning | ⚠️ | `rgba(245,158,11,0.12)` | `#F59E0B` |
| Info | ℹ️ | `rgba(6,182,212,0.12)` | `#06B6D4` |

**位置**: 右上角  
**持续时间**: 3秒  
**圆角**: 8px

---

## 三、业务组件

### 3.1 GPUCard GPU卡片

```
┌─────────────────────────┐
│  RTX 4090         🔥热门 │
│  24GB GDDR6X · 入门首选 │
│  ─────────────────────  │
│  CUDA 核心    16,384    │
│  显存带宽     1,008 GB/s│
│  FP32 算力    82.6 TFLOPS│
│  推荐场景     模型推理   │
│  ─────────────────────  │
│       ¥2.5 /小时         │
│  [ 创建实例 ]            │
└─────────────────────────┘
```

**尺寸**: 宽320px, 高400px  
**悬浮效果**: 上移4px + 蓝色边框

### 3.2 InstanceRow 实例行

```
┌────────────────────────────────────────────────────┐
│ inst-a8f3k2 │ A100 80G │ 🟢运行中 │ 10.0.3.12 │ ... │
│ 14h 32m    │ ¥116.00 │ SSH · 监控 · 停止          │
└────────────────────────────────────────────────────┘
```

**行高**: 72px  
**操作列**: 链接样式，用 `·` 分隔

### 3.3 MetricChart 监控图表

| 图表类型 | 用途 | 库 |
|----------|------|----|
| 折线图 | GPU利用率趋势 | Chart.js |
| 仪表盘 | 温度/功耗 | Chart.js (doughnut) |
| 面积图 | 网络流量 | Chart.js |
| 柱状图 | 显存使用 | Chart.js |

---

## 四、页面布局规范

### 4.1 控制台布局
```
┌────────────────────────────────────────┐
│  Navbar (固定顶部, 64px)               │
├──────────┬─────────────────────────────┤
│ Sidebar  │  Content Area               │
│ (240px)  │  (自适应)                   │
│          │                             │
│ 仪表盘   │  [统计卡片]                 │
│ 实例     │  [实例表格]                 │
│ 账单     │                             │
│ 监控     │                             │
│ 设置     │                             │
└──────────┴─────────────────────────────┘
```

### 4.2 间距规范
- 页面内边距: 24px
- 卡片间距: 24px
- 卡片内边距: 24px
- 统计卡片间距: 16px

---

*设计系统文档结束*
